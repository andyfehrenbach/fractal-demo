{
  "name": "fractal-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-fractal": "fractal start --sync",
    "build": "npm run preprocess && npm run start-fractal",
  "preprocess": "suitcss src/public/index.css build/build.css",
  "setup": "npm install && mkdir -p build"
  },
  "author": "andy fehrenbach",
  "license": "ISC",
  "dependencies": {
    "@frctl/fractal": "^1.1.3",
    "suitcss": "^2.0.0",
    "suitcss-preprocessor": "^3.0.1"
  }
}
